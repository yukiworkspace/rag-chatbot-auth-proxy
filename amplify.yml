version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting Amplify build for IP authentication proxy"
    build:
      commands:
        - echo "Static site build - no build process required"
        - echo "Deployment timestamp:" $(date)
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
